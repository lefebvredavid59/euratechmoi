{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <section id="carousel mt-0">
        <div id="euraCarousel" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                {% set isFirst = true %}
                {% for carousel in carousels %}
                {% if isFirst == true %}
                    <div class="carousel-item active data-interval="500">
                {% else %}
                    <div class="carousel-item data-interval="500">
                {% endif %}
                <img class="d-block w-100" src="{{ carousel.image }}" alt="{{ carousel.titre }}">
                <div class="carousel-caption">
                    <div class="caption-text">
                        <h2 class="text-info display-4">{{ carousel.titre }}</h2>
                        <p class="text-warning lead">{{ carousel.description }}</p>
                    </div>
                </div>
            </div>
            {% set isFirst = false %}
            {% endfor %}
            {% set isFirst = true %}
            {% for carousel in carousels %}
                {% if isFirst == true %}
                    <a class="carousel-control-prev" href="#euraCarousel" role="button" data-slide="{{ loop.index0 }}">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                {% else %}
                    <a class="carousel-control-next" href="#euraCarousel" role="button" data-slide="{{ loop.index0 }}">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                {% endif %}
                {% set isFirst = false %}
            {% endfor %}
        </div>
    </section>
    <section>
        <div class="container py-5 text-center" id="intro">
            <hr>
            <p class="lead pt-4 pb-3">EuraTechnologies renforce son <span>programme d’accompagnement</span> des plus
                jeunes avec <span>EURATECH’KIDS</span> pour donner aux enfants le goût de la programmation et les
                projeter dans le futur.</p>
            <a href="#" class="btn btn-info btn-block">Nous découvrir</a>
        </div>
    </section>
    <section id="test">
        <article id="ateliers">
            <h2 class="display-4 text-warning text-center pt-5">Les ateliers</h2>
            <div class="row py-5 container mx-auto">
                {% for programme in programmes %}
                    <div class="col-12 col-lg-4 pb-3 mx-auto">
                        <div class="card mx-auto border-0" id="at1" style="width: 18rem;">
                            <div class="card-header" id="at1-h">
                                <h5 class="text-center">{{ programme.titre }}</h5>
                            </div>
                            <div class="card-body">
                                <h5 class="card-title text-center">{{ programme.categorie }}</h5>
                                <p class="card-text">{{ programme.description }}</p>
                                <a href="#" class="btn btn-block btn-eura">En savoir plus</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </article>
    </section>
    <section id="contact">
        <div class="row px-5">
            <div class="col-12 col-lg-6 pt-5">
                <h2>Envoyer un message</h2>
                <form class="pb-3">
                    <div class="form-group">
                        <label for="email">Adresse mail</label>
                        <input type="email" class="form-control" id="email" aria-describedby="emailHelp">
                        <small id="emailHelp" class="form-text text-muted">Nous ne partagerons jamais cette adresse avec
                            quelqu'un d'autre.</small>
                    </div>
                    <div class="form-group">
                        <label for="nom">Votre nom</label>
                        <input type="text" class="form-control" id="nom">
                    </div>
                    <div class="form-group">
                        <label for="tel">Votre téléphone</label>
                        <input type="tel" class="form-control" id="tel"
                               pattern="[0-9]{2}[0-9]{2}[0-9]{2}[0-9]{2}[0-9]{2}" aria-describedby="telHelp">
                        <small id="telHelp" class="form-text text-muted">Exemple: 0612345678</small>
                    </div>
                    <div class="form-group">
                        <label for="message">Votre Message</label>
                        <textarea name="message" id="message" cols="30" rows="5" class="form-control" id="message"
                                  style="resize: none;"></textarea>
                    </div>
                    <button type="submit" class="btn btn-outline-eura">Envoyer votre message</button>
                </form>
            </div>
            <div class="col-12 col-lg-4 mx-auto pt-5">
                <h2>Siège</h2>
                <p>Cyber-espace EuraTechnologies</p>
                <p>165 Avenue de Bretagne <br/>59000 Lille<br/><i>FRANCE</i></p>
            </div>
        </div>
        <div class="row">
            <div class="col-12 p-0" id="map">
                <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2530.6812896777224!2d3.0178653157380126!3d50.633037179501024!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x47c2d55c764ab439%3A0x2e9bfd5c48d283b!2sEuraTechnologies!5e0!3m2!1sfr!2sfr!4v1580645598517!5m2!1sfr!2sfr"
                        width="100%" height="600" frameborder="0" style="border:0;" allowfullscreen=""></iframe>
            </div>
        </div>
    </section>
    </div>
{% endblock %}
